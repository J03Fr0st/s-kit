# Action Required: Superpowers Dev Port Review

Manual steps that may be needed during implementation.

## Before Implementation

- [ ] **Confirm Codex hook direction if compatibility is unclear** — if the Codex app/plugin hook contract cannot be verified from local docs or installed plugin behavior, pause before adding native hook files.

## During Implementation

- [ ] **Approve any test deletion** — no existing bash or plugin test should be removed unless the coverage map proves its assertions are represented elsewhere.

## After Implementation

- [ ] **Decide whether eval-harness work becomes its own follow-up design** — this feature should produce an eval strategy, not silently migrate the test architecture.

---

> These tasks are also referenced in context within the relevant task files.
