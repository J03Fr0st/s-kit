# s-kit Release Notes

## Unreleased

- Imported the full base skills/plugin structure.
- Rebranded user-facing surfaces to `s-kit`.
- Added `plan-feature` and `build-feature` as the canonical planning and implementation skills.
- Set the canonical workflow to `brainstorming -> plan-feature -> build-feature -> verification/review -> ship`.
- Standardized feature specs under `docs/specs/YYYY-MM-DD-{feature-name}/`.
