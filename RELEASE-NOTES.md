# s-kit Release Notes

## Unreleased

- Imported the full base skills/plugin structure.
- Rebranded user-facing surfaces to `s-kit`.
- Added `create-spec` and `implement-feature`.
- Set the canonical workflow to `brainstorming -> create-spec -> implement-feature -> verification/review -> ship`.
- Standardized feature specs under `docs/specs/YYYY-MM-DD-{feature-name}/`.
