# Core

- Repo purpose from `README.md`: `s-kit` is a kit for the user's agentic workflow.
- Current tracked source is intentionally minimal: `LICENSE` and `README.md`; `.serena/` was generated locally for Serena project state.
- Read `mem:tech_stack` before assuming build tooling or language servers; there is no application/runtime structure yet.
- Read `mem:suggested_commands` for the few useful project commands on Windows.
- Read `mem:conventions` before adding workflow content; current convention is document-first until code/tooling exists.
- Read `mem:task_completion` before finishing changes; verification is currently repository hygiene, not build/test execution.