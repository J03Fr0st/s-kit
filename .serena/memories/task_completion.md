# Task Completion

- For documentation-only changes: inspect the changed Markdown and run `git diff --check`.
- For repo hygiene: run `git status --short --branch` and confirm only intentional files changed.
- If Serena memories were added, deleted, renamed, or references changed, run `serena memories check` from the project root.
- Add build/test/lint commands here only after the repo gains actual tooling.