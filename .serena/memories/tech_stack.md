# Tech Stack

- No package manifest, build config, test runner, or application source exists yet.
- Durable source content is Markdown plus license text.
- Serena project config is at `.serena/project.yml`; it was configured for `markdown` because the repo currently only has documentation content.
- `.serena/project.local.yml` is local-only override space; `.serena/.gitignore` excludes `/cache` and `/project.local.yml`.